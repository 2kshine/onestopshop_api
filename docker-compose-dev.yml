version: "2"
services:
  node-app:
    build:
      context: .
      args:
        NODE_ENV: development
    volumes:
      - ./:/app
      - /app/node_modules    
    command: npm run start

  mydb:
    volumes:
      - my-db:/var/lib/mysql
    ports:
      - "3306:3306"
volumes:
  my-db:
    